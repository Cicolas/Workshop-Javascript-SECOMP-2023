// Elemento:
// ---------
// <li id="item-0" class="item">
//   <input type="checkbox"/>
//   <input class="descricao" placeholder="descrição"></input>
//   <i onclick="removerItem(0)" class="ph ph-trash"></i>
// </li>

// -----------------------------------------------------------------------------

// ? SELECIONAR A LISTA

function novoItem() {
  // ? CRIAR O NOVO ITEM
  // ? CONFIGURAR O ITEM
  // ? ADICIONAR A LISTA O NOVO ITEM
}

function removerItem(id) {
  // ? SELECIONAR O ITEM
  // ? REMOVER O ITEM
}

function limparLista() {
  // ? SELECIONAR TODOS OS ITENS
  // ? VERIFICAR UM POR UM SE ESTA SELECIONADO
  // ? REMOVER O ITEM
}